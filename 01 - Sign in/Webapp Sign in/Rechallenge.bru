meta {
  name: Rechallenge Webapp
  type: http
  seq: 1
}

post {
  url: {{API_ROOT}}
}

headers {
  content-type: application/json
}

body {
  {
    "jsonrpc": "2.0+hl",
    "id": 1,
    "method": "hlwebapp:sign_in:rechallenge",
    "version": 1,
    "params": {
     "rechallenge_token": "{{RECHALLENGE_TOKEN}}"
    },
    "auth": {}
  }
}

tests {
  test("status must be 200", function() {
    expect(response.status).to.eql(200);
  });
}

script:post-response {
  bru.setVar("REPLY_TOKEN", res.body.result.reply_token);
  bru.setVar("RECHALLENGE_TOKEN", res.body.result.rechallenge_token);
}